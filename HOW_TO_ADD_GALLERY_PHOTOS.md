# 📸 طريقة إضافة صور معرض الزفاف على الفايربيس (How to Add Gallery Photos to Firebase)

يشرح هذا الدليل خطوة بخطوة كيفية إضافة ورفع صور العروسين إلى معرض الصور الخاص بموقع الزفاف من خلال **Firebase Console**.

---

## 🛠️ الطريقة 1: إضافة الصور عبر رابط مباشر (Firestore Collection)

إذا كانت الصور المرفوعة مسبقاً على أي موقع أو سيرفر (مثل Imgur أو Cloudinary أو أي رابط أونلاين):

1. افتح متصفح الإنترنت وانتقل إلى [Firebase Console](https://console.firebase.google.com/).
2. اختر مشروع الزفاف الخاص بك (`wedding-invitation-2026`).
3. من القائمة الجانبية اختر **"Firestore Database"**.
4. اضغط على **"Start collection" (بدء مجموعة جديدة)** (أو اختر المجموعة `gallery` إذا كانت موجودة).
5. اكتب اسم المجموعة: **`gallery`** ثم اضغط **"Next"**.
6. في نافذة إضافة المستند (Add Document):
   - اضغط **"Auto-ID"** لإنشاء معرف تلقائي للمستند.
   - أضف الحقول الاربعة التالية بالدقة التالية:

| اسم الحقل (Field Name) | النوع (Type) | المثال / القيمة المقبولة | الوصف |
| :--- | :--- | :--- | :--- |
| **`url`** | string | `https://example.com/photo1.jpg` | رابط الصورة المباشر |
| **`alt`** | string | `حمدى ورودينا — جلسة تصوير بورسعيد` | وصف الصورة للضيوف |
| **`aspectRatio`** | string | `portrait` أو `landscape` أو `square` | شكل الصورة لتنسيق المعرض |
| **`order`** | number | `1` (أو `2`, `3`, `4`...) | ترتيب ظهور الصورة في المعرض |

7. اضغط **"Save" (حفظ)**.
8. كرر الخطوة لكل صورة تريد إضافتها لمعرض الزفاف!

---

## ☁️ الطريقة 2: رفع الصور المباشرة من جهازك (Firebase Storage)

إذا كانت الصور موجودة على كمبيوترك أو هاتفك وتريد رفعها على الفايربيس:

### الخطوة أ: تفعيل Firebase Storage
1. من القائمة الجانبية في Firebase Console، اختر **"Build"** ➔ **"Storage"**.
2. اضغط على **"Get started"**.
3. اختر وضع التفعيل **"Start in test mode"** ثم اضغط **"Next"** ➔ **"Done"**.

### الخطوة ب: رفع الصور واستخراج الرابط
1. داخل صفحة **Storage**، اضغط زر **"Upload file" (رفع ملف)**.
2. اختر صور العروسين من جهازك.
3. بعد اكتمال الرفع، اضغط على اسم الصورة في القائمة لفتح تفاصيلها.
4. من القائمة الجانبية اليسرى للتفاصيل، اضغط على **"File location"**.
5. اضغط على **"Download URL"** لنسخ الرابط المباشر للصورة.

### الخطوة ج: ربط الرابط في Firestore
1. اذهب إلى **Firestore Database** ➔ مجموعة **`gallery`**.
2. أنشئ مستند جديد بأقسام:
   - `url`: يلصق فيه رابط الـ Download URL الذي نسخته في الخطوة ب.
   - `alt`: اسم الصورة.
   - `aspectRatio`: `portrait` (طولي) / `landscape` (عرضي) / `square` (مربع).
   - `order`: رقم الترتيب.
3. اضغط **Save**.

---

## 🎨 أمثلة لتنسيق الأبعاد (`aspectRatio`)

يستخدم موقع الزفاف شبكة أنيقة متغيرة الأبعاد (Masonry Grid)، يمكنك اختيار الشكل الأنسب لكل صورة:
- **`portrait`**: للصور الطولية (مثل صور العروسين بالكامل).
- **`landscape`**: للصور العرضية (مثل صور قاعة الحفل أو البحر).
- **`square`**: للصور المربعة (مثل صور التفاصيل، الخواتم، الورود).

---

✨ **بمجرد حفظ الصور في الفايربيس، ستظهر تلقائياً ولحظياً في سكشن معرض الصور بالموقع!**
