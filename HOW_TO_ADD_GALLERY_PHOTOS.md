# 📸 طريقة إضافة صور معرض الزفاف (How to Add Gallery Photos)

يشرح هذا الدليل الطرق المتاحة لإضافة صور العروسين إلى معرض الصور الخاص بموقع الزفاف من خلال **لوحة التحكم المباشرة (Admin Portal)** أو من خلال **Firebase Console**.

---

## 🌟 الطريقة 1 (الأسهل والأسرع): من خلال لوحة التحكم (Admin Portal)

1. افتح لوحة التحكم السرية:
   * محلياً: [http://localhost:5175/#admin-secret](http://localhost:5175/#admin-secret)
   * بعد النشر: `https://hamdyKouta1.github.io/invitation/#admin-secret`
2. سجّل الدخول بالبريد وكلمة المرور ثم أدخل رمز التحقق OTP.
3. انتقل إلى تبويب **📸 معرض الصور**.
4. في نموذج الإضافة:
   * **رابط الصورة:** الصق رابط الصورة المباشر أو رابط ملف Google Drive مثل:
     `https://drive.google.com/file/d/1Tse-ZniYpa1yU7p2wvn6P8wbqYmL.../view`
     *(يقوم النظام بتحويل رابط Google Drive تلقائياً لعرضه بجودة عالية دون أي إعداد إضافي!)*
   * **ترتيب ظهور الصورة (Order):** أدخل رقم الترتيب كعدد صحيح (مثل `1`, `2`, `3`...).
   * **أبعاد الصورة (Aspect Ratio):** اختر `طولية (Portrait 3:4)` أو `عرضية (Landscape 4:3)` أو `مربعة (Square 1:1)`.
   * **وصف الصورة (Alt Text):** وصف اختياري للصورة (مثل "جلسة تصوير بورسعيد").
5. اضغط **"إضافة الصورة لمعرض Firestore"** ➔ ستُحفظ الصورة فوراً في قاعدة بيانات Firebase وتظهر مباشرة لضيوف الموقع!

---

## 🛠️ الطريقة 2: من خلال Firebase Console مباشرة

1. افتح [Firebase Console](https://console.firebase.google.com/).
2. اختر المشروع (`wedding-invitation-2026`).
3. من القائمة الجانبية اختر **"Firestore Database"** ➔ مجموعة **`gallery`**.
4. اضغط **"Add document"** (Auto-ID) وأضف الحقول التالية:

| اسم الحقل (Field Name) | النوع (Type) | المثال / القيمة | الوصف |
| :--- | :--- | :--- | :--- |
| **`url`** | string | `https://drive.google.com/file/d/1Tse-...` | رابط الصورة أو رابط Google Drive |
| **`order`** | number (int) | `1` (أو `2`, `3`, `4`...) | ترتيب ظهور الصورة في المعرض |
| **`aspectRatio`** | string | `portrait` أو `landscape` أو `square` | شكل الصورة لتنسيق المعرض |
| **`alt`** | string | `حمدى ورودينا — جلسة تصوير بورسعيد` | وصف الصورة |

5. اضغط **"Save" (حفظ)**.

---

## 🎨 أمثلة لتنسيق الأبعاد (`aspectRatio`)

يستخدم موقع الزفاف شبكة أنيقة متغيرة الأبعاد (Masonry Grid)، يمكنك اختيار الشكل الأنسب لكل صورة:
- **`portrait`**: للصور الطولية (مثل صور العروسين بالكامل).
- **`landscape`**: للصور العرضية (مثل صور قاعة الحفل أو البحر).
- **`square`**: للصور المربعة (مثل صور التفاصيل، الخواتم، الورود).

---

✨ **بمجرد حفظ الصور في الفايربيس أو لوحة التحكم، ستظهر تلقائياً ولحظياً في سكشن معرض الصور بالموقع!**

